import java.util.Scanner;

public class arrays {
}
    public static void main(String[]) args) {
        Scanner io =
        System.out.print("Enter a word: ");
        String word = io.nextLine();

        //array
        int[] nums = new int[3];

        nums[0] = 42;
        nums [1] = 13;
        nums[2] = 7;


        int[] nums2 = {1, 2, 3, 4, 5}

         for (int i = 0; i <nums2.length; i ++) {
             System.out.println(nums2[i]);

        }
         for (int i = num2 length - 1; i > = 0; i--) {

             System.out.println(nums2[i]);
        }

        String[] colors;

         while (true) {
             System.out.print("Enter a color ("Q or quit: ");

        if (totalColors > colors.length) {
            //creates a new temp array that's 2x bigger
            String [] tmep = new String[colors.length * 2]
            String color = io.netxtLine();
          //done entering data
            if(collor.eguals("Q")) {
                break
            } else {
                colors[totalColors - 1] = color ;
            }

        }
                 }for (int i  = 0; i < colors)
        }